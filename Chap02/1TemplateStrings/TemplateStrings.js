var First_Name = 'Phạm Tiến';
var Last_Name = 'Mạnh';

const email = `
Xin chào ${First_Name} ${Last_Name}. 
Đang test thử template strings.
Thử xem xuống dòng k.
Thks ${First_Name} ${Last_Name}
`;

console.log(email);